/*
SDK Version Notice Partial
*/

import Admonition from '@theme/Admonition';

export const SdkVersion = () => (
  <Admonition type="info" title="SDK Version">
    <p>
      This guide is written and tested against <strong>Bittensor v11</strong>, the unified package containing the Python SDK, <code>btcli</code>, and the wallet module. Upgrading from v10 or earlier? See the <a href="/sdk/migration-guide">v10 to v11 migration guide</a>. For documentation built against SDK 9.12, see the <a href="https://archive-sdk-9-12.developer-docs-6uq.pages.dev/sdk/bt-api-ref" target="_blank" rel="noopener noreferrer">SDK 9.12 Archive</a>.
    </p>
  </Admonition>
);

